# Replace YOUR_DOMAIN with your actual domain (e.g., assetra.com)
YOUR_DOMAIN {
    # API requests -> backend
    handle /api/* {
        reverse_proxy backend:8080
    }

    # Everything else -> frontend
    handle {
        reverse_proxy frontend:3000
    }
}
