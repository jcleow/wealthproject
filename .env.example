# Env templates
# - For local dev: copy .env.dev.example -> .env.dev (and optionally ln -s .env.dev .env)
# - For prod: copy .env.prod.example -> .env.prod
# Tools that expect .env (e.g., run-ui) will work if you symlink or copy the appropriate file.
